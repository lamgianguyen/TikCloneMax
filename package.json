{
  "name": "clone",
  "version": "1.0.0",
  "description": "TikFinity local clone",
  "main": "index.js",
  "scripts": {
    "web": "dotnet run --project backend/TikFinityBackend.csproj --no-launch-profile",
    "desktop": "npm --prefix electron start",
    "desktop:build": "npm --prefix electron run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "socket.io": "^4.8.3",
    "ws": "^8.19.0"
  }
}
